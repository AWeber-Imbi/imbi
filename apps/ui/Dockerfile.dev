# Dev image for the UI in the Okteto environment; `okteto up imbi-ui`
# syncs ./ui over /workspace/apps/ui and runs the Vite dev server.
FROM node:24-slim

WORKDIR /workspace/apps/ui
COPY ./ /workspace/apps/ui
RUN npm install
