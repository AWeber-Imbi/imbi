apiVersion: v2
name: imbi
description: DevOps Service Management Platform
type: application
version: 2.0.0
appVersion: "2.0.0"

# Imbi expects PostgreSQL (with Apache AGE) and ClickHouse to be provisioned
# externally — see docs/deployment/kubernetes.md. The chart bundles no
# database subcharts.
